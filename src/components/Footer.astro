---
const currentYear = new Date().getFullYear();

const quickLinks = [
  { href: '/projects', label: 'Projects' },
  { href: 'https://github.com/Bayega-Joan', label: 'Github', external: true },
  { href: 'https://devpost.com/bayegajoan8?ref_content=user-portfolio&ref_feature=portfolio&ref_medium=global-nav', label: 'Hackathons', external: true },
];
---

<footer class="footer">
  <div class="container">
    <div class="footer-content">
      <div class="footer-section">
        <h4>Topics Of Interest</h4>
        <p>
          Principled Innovation, Social Entrepreneurship, Accessibility and Inclusion,
          Educational Technologies, Computer Vision and Robotics.
        </p>
        <p class="footer-links">
          Let's connect on <a href="https://www.linkedin.com/in/joanbayega/" target="_blank" rel="noopener noreferrer">LinkedIn</a> || Download my resume <a href="https://drive.google.com/file/d/1zI0xPfq6GmChFU6PRXlPKUm0PMEv_ur2/view?usp=sharing" target="_blank" rel="noopener noreferrer">here</a>
        </p>
      </div>

      <div class="footer-section">
        <h4>Quick Links</h4>
        <ul class="quick-links">
          {quickLinks.map(link => (
            <li><a href={link.href} target={link.external ? "_blank" : "_self"} rel={link.external ? "noopener noreferrer" : undefined}>{link.label}</a></li>
          ))}
        </ul>
      </div>
    </div>

    <div class="footer-bottom">
      <p>Copyright © {currentYear} Bayega Joan.</p>
      <button class="back-to-top" aria-label="Back to top" id="backToTop">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M18 15l-6-6-6 6"/>
        </svg>
      </button>
    </div>
  </div>
</footer>

<script>
  document.getElementById('backToTop')?.addEventListener('click', () => {
    window.scrollTo({ top: 0, behavior: 'smooth' });
  });
</script>

<style>
  .footer {
    background-color: var(--color-footer-bg);
    color: var(--color-footer-text);
    padding: 60px 0 30px;
  }

  .footer-content {
    display: grid;
    grid-template-columns: 1.5fr 1fr;
    gap: 60px;
    padding-bottom: 40px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .footer-section h4 {
    color: var(--color-heading);
    color: white;
    font-size: 1.125rem;
    margin-bottom: 20px;
  }

  .footer-section p {
    font-size: 0.9375rem;
    line-height: 1.7;
    margin-bottom: 15px;
  }

  .footer-links a {
    color: var(--color-primary);
  }

  .footer-links a:hover {
    text-decoration: underline;
  }

  .quick-links {
    list-style: none;
  }

  .quick-links li {
    padding: 10px 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .quick-links li:first-child {
    padding-top: 0;
  }

  .quick-links a {
    color: var(--color-footer-text);
    font-size: 0.9375rem;
    transition: color var(--transition-fast);
  }

  .quick-links a:hover {
    color: var(--color-primary);
  }

  .footer-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: 30px;
  }

  .footer-bottom p {
    font-size: 0.875rem;
    margin: 0;
  }

  .back-to-top {
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: rgba(255, 255, 255, 0.1);
    border: none;
    border-radius: 4px;
    color: var(--color-footer-text);
    cursor: pointer;
    transition: all var(--transition-fast);
  }

  .back-to-top:hover {
    background-color: var(--color-primary);
    color: white;
  }

  @media (max-width: 768px) {
    .footer-content {
      grid-template-columns: 1fr;
      gap: 40px;
    }
  }
</style>
